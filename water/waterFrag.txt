#version 330 core
in GEO_OUT
{
  vec3 wNormal;
} geo_out;
out vec4 FragColor;
void main()
{
	vec3 worldNormal = geo_out.wNormal;
	FragColor = vec4(abs(worldNormal.x),abs(worldNormal.y),abs(worldNormal.z), 1.0);
}  