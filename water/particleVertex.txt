#version 330 core
layout (location = 0) in vec3 pos;
uniform mat4 projection;
uniform mat4 view;
uniform mat4 model;
void main() {
    gl_Position = projection * view * model * vec4((pos + vec3(gl_InstanceID / 100.0f)), 1.0);
	//gl_Position = projection * view * model * vec4(pos, 1);
}